name: test
on: [push, pull_request]

jobs:
  tests:
    name: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      
      - name: Install yq - yaml parser
        run: pip3 install yq

      - name: read
        id: read
        run: yq '."13.4".engine-branch' .github/version-branch-template.yml
        
      - name: final
        id: final 
        run: echo "done"
